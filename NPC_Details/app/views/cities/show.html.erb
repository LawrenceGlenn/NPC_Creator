<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @city.name %>
</p>

<p>
  <strong>Population:</strong>
  <%= @city.population %>
</p>

<p>
  <strong>NPCs:</strong>
  <% Npc.find(@city.npc_ids).each do | tempNPC| %>
      <%= tempNPC.name %>, 
    <% end %>
</p>

<%= render 'family_chart', city: @city %>

<%= link_to 'Edit', edit_city_path(@city) %> |
<%= link_to 'Back', cities_path %>
